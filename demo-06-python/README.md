# Demo 06: Python

Explain that F3D is also a C++ library that can be used to integrate its reading and rendering capabilities into existing application.
Explain the C++ library is also bound to high level languages: Python, Java, JS but also C in order to be integrated into virtually all languages (e.g. Rust, Julia)

Make sure the virtual environment is setup, then run `python ./demo.py`
